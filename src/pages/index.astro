---

---

<div class="ba-wrapper">
    <div class="ba-container animate-fade-in">
        <div class="ba-side ba-before-side" role="group" aria-label="Before image">
            <div class="ba-label">Before</div>
            <img class="ba-img" src={Astro.props.before ?? '/images/hero.svg'} alt={Astro.props.caption ?? 'Before'} />
        </div>

        <div class="ba-side ba-after-side" role="group" aria-label="After image">
            <div class="ba-label">After</div>
            <img class="ba-img" src={Astro.props.after ?? '/images/service.svg'} alt={Astro.props.caption ?? 'After'} />
        </div>
    </div>
    {Astro.props.caption ? <p class="ba-caption">{Astro.props.caption}</p> : ''}
</div>
pasted it in
